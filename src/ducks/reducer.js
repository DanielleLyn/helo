const initialState ={
    username: 'hello',
    id: '', 
    profilePic: '',
};

export default (state = initialState, action) => {
    switch (action.type){
        default:
        return state;
    }
};
